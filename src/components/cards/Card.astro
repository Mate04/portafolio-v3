---
import { cn } from "../../lib/utils";

export interface Props {
  class?: string;
  [key: string]: any;
}
//class=""
const { class: className = "", ...rest } = Astro.props;
---
<div class={cn("rounded-xl border border-gray-200 bg-white shadow-sm", className)} {...rest}>
  <slot />
</div>
